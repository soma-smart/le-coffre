apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "le-coffre.frontend.fullname" . }}-config
  labels:
    {{- include "le-coffre.frontend.labels" . | nindent 4 }}
data:
  config.js: |
    window.__APP_CONFIG__ = {
      apiBaseUrl: {{ .Values.frontend.config.apiBaseUrl | quote }}
    };
