[project]
name = "Le-Coffre"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi[standard]>=0.116.1",
  "PyCryptodome==3.23.0",
]

[dependency-groups]
dev = ["pytest==7.4.4", "pytest-watcher==0.4.3"]

[tool.pytest.ini_options]
pythonpath = "src"
