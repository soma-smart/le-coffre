// This file is auto-generated by @hey-api/openapi-ts

import type { Client, ClientMeta, Options as Options2, RequestResult, TDataShape } from './client';
import { client } from './client.gen';
import type { AddMemberToGroupGroupsGroupIdMembersPostData, AddMemberToGroupGroupsGroupIdMembersPostErrors, AddMemberToGroupGroupsGroupIdMembersPostResponses, AddOwnerToGroupGroupsGroupIdOwnersPostData, AddOwnerToGroupGroupsGroupIdOwnersPostErrors, AddOwnerToGroupGroupsGroupIdOwnersPostResponses, AdminLoginAuthLoginPostData, AdminLoginAuthLoginPostErrors, AdminLoginAuthLoginPostResponses, ApproveExtensionPairingExtensionPairingUserCodeApprovePostData, ApproveExtensionPairingExtensionPairingUserCodeApprovePostErrors, ApproveExtensionPairingExtensionPairingUserCodeApprovePostResponses, ClearPendingSharesVaultUnlockClearDeleteData, ClearPendingSharesVaultUnlockClearDeleteResponses, ConfigureSsoProviderAuthSsoConfigurePostData, ConfigureSsoProviderAuthSsoConfigurePostErrors, ConfigureSsoProviderAuthSsoConfigurePostResponses, ConsumeOneTimeLinkOneTimeLinksConsumePostData, ConsumeOneTimeLinkOneTimeLinksConsumePostErrors, ConsumeOneTimeLinkOneTimeLinksConsumePostResponses, CreateGroupGroupsPostData, CreateGroupGroupsPostErrors, CreateGroupGroupsPostResponses, CreateOneTimeLinkPasswordsPasswordIdOneTimeLinksPostData, CreateOneTimeLinkPasswordsPasswordIdOneTimeLinksPostErrors, CreateOneTimeLinkPasswordsPasswordIdOneTimeLinksPostResponses, CreatePasswordPasswordsPostData, CreatePasswordPasswordsPostErrors, CreatePasswordPasswordsPostResponses, CreateUserUsersPostData, CreateUserUsersPostErrors, CreateUserUsersPostResponses, CreateVaultVaultSetupPostData, CreateVaultVaultSetupPostErrors, CreateVaultVaultSetupPostResponses, DeleteGroupGroupsGroupIdDeleteData, DeleteGroupGroupsGroupIdDeleteErrors, DeleteGroupGroupsGroupIdDeleteResponses, DeletePasswordPasswordsPasswordIdDeleteData, DeletePasswordPasswordsPasswordIdDeleteErrors, DeletePasswordPasswordsPasswordIdDeleteResponses, DeleteUserUsersUserIdDeleteData, DeleteUserUsersUserIdDeleteErrors, DeleteUserUsersUserIdDeleteResponses, DenyExtensionPairingExtensionPairingUserCodeDenyPostData, DenyExtensionPairingExtensionPairingUserCodeDenyPostErrors, DenyExtensionPairingExtensionPairingUserCodeDenyPostResponses, ExchangeExtensionDeviceExtensionDeviceExchangePostData, ExchangeExtensionDeviceExtensionDeviceExchangePostErrors, ExchangeExtensionDeviceExtensionDeviceExchangePostResponses, GetCsrfTokenAuthCsrfTokenGetData, GetCsrfTokenAuthCsrfTokenGetResponses, GetExtensionPairingExtensionPairingUserCodeGetData, GetExtensionPairingExtensionPairingUserCodeGetErrors, GetExtensionPairingExtensionPairingUserCodeGetResponses, GetExtensionSessionExtensionSessionGetData, GetExtensionSessionExtensionSessionGetResponses, GetGroupGroupsGroupIdGetData, GetGroupGroupsGroupIdGetErrors, GetGroupGroupsGroupIdGetResponses, GetPasswordPasswordsPasswordIdGetData, GetPasswordPasswordsPasswordIdGetErrors, GetPasswordPasswordsPasswordIdGetResponses, GetPasswordStatisticForAdminPasswordsStatisticsGetData, GetPasswordStatisticForAdminPasswordsStatisticsGetResponses, GetSsoUrlAuthSsoUrlGetData, GetSsoUrlAuthSsoUrlGetErrors, GetSsoUrlAuthSsoUrlGetResponses, GetStatisticForAdminIamStatisticsGetData, GetStatisticForAdminIamStatisticsGetResponses, GetUserMeUsersMeGetData, GetUserMeUsersMeGetResponses, GetUserUsersUserIdGetData, GetUserUsersUserIdGetErrors, GetUserUsersUserIdGetResponses, GetVaultStatusVaultStatusGetData, GetVaultStatusVaultStatusGetResponses, HealthCheckHealthGetData, HealthCheckHealthGetResponses, IsSsoConfigSetAuthSsoIsConfiguredGetData, IsSsoConfigSetAuthSsoIsConfiguredGetResponses, ListExtensionGroupsExtensionGroupsGetData, ListExtensionGroupsExtensionGroupsGetResponses, ListExtensionTokensExtensionTokensGetData, ListExtensionTokensExtensionTokensGetResponses, ListGroupsGroupsGetData, ListGroupsGroupsGetErrors, ListGroupsGroupsGetResponses, ListMyOneTimeLinksOneTimeLinksMineGetData, ListMyOneTimeLinksOneTimeLinksMineGetErrors, ListMyOneTimeLinksOneTimeLinksMineGetResponses, ListOneTimeLinksForAdminAdminOneTimeLinksGetData, ListOneTimeLinksForAdminAdminOneTimeLinksGetErrors, ListOneTimeLinksForAdminAdminOneTimeLinksGetResponses, ListOneTimeLinksPasswordsPasswordIdOneTimeLinksGetData, ListOneTimeLinksPasswordsPasswordIdOneTimeLinksGetErrors, ListOneTimeLinksPasswordsPasswordIdOneTimeLinksGetResponses, ListPasswordAccessPasswordsPasswordIdAccessGetData, ListPasswordAccessPasswordsPasswordIdAccessGetErrors, ListPasswordAccessPasswordsPasswordIdAccessGetResponses, ListPasswordEventsByActorAdminUsersUserIdPasswordEventsGetData, ListPasswordEventsByActorAdminUsersUserIdPasswordEventsGetErrors, ListPasswordEventsByActorAdminUsersUserIdPasswordEventsGetResponses, ListPasswordEventsPasswordsPasswordIdEventsGetData, ListPasswordEventsPasswordsPasswordIdEventsGetErrors, ListPasswordEventsPasswordsPasswordIdEventsGetResponses, ListPasswordsPasswordsListGetData, ListPasswordsPasswordsListGetErrors, ListPasswordsPasswordsListGetResponses, ListUsersUsersGetData, ListUsersUsersGetResponses, LockVaultVaultLockPostData, LockVaultVaultLockPostResponses, LogoutAuthLogoutPostData, LogoutAuthLogoutPostErrors, LogoutAuthLogoutPostResponses, PromoteUserToAdminUsersUserIdPromoteAdminPostData, PromoteUserToAdminUsersUserIdPromoteAdminPostErrors, PromoteUserToAdminUsersUserIdPromoteAdminPostResponses, ReadinessCheckHealthReadyGetData, ReadinessCheckHealthReadyGetResponses, RefreshAccessTokenAuthRefreshTokenPostData, RefreshAccessTokenAuthRefreshTokenPostErrors, RefreshAccessTokenAuthRefreshTokenPostResponses, RegisterAdminAuthRegisterAdminPostData, RegisterAdminAuthRegisterAdminPostErrors, RegisterAdminAuthRegisterAdminPostResponses, RegisterExtensionDeviceExtensionDevicePostData, RegisterExtensionDeviceExtensionDevicePostErrors, RegisterExtensionDeviceExtensionDevicePostResponses, RemoveMemberFromGroupGroupsGroupIdMembersUserIdDeleteData, RemoveMemberFromGroupGroupsGroupIdMembersUserIdDeleteErrors, RemoveMemberFromGroupGroupsGroupIdMembersUserIdDeleteResponses, RevokeAllExtensionTokensExtensionTokensDeleteData, RevokeAllExtensionTokensExtensionTokensDeleteResponses, RevokeAllOneTimeLinksForUserAdminUsersUserIdOneTimeLinksDeleteData, RevokeAllOneTimeLinksForUserAdminUsersUserIdOneTimeLinksDeleteErrors, RevokeAllOneTimeLinksForUserAdminUsersUserIdOneTimeLinksDeleteResponses, RevokeExtensionTokenExtensionTokensTokenIdDeleteData, RevokeExtensionTokenExtensionTokensTokenIdDeleteErrors, RevokeExtensionTokenExtensionTokensTokenIdDeleteResponses, RevokeOneTimeLinkForAdminAdminOneTimeLinksLinkIdDeleteData, RevokeOneTimeLinkForAdminAdminOneTimeLinksLinkIdDeleteErrors, RevokeOneTimeLinkForAdminAdminOneTimeLinksLinkIdDeleteResponses, RevokeOneTimeLinkOneTimeLinksLinkIdDeleteData, RevokeOneTimeLinkOneTimeLinksLinkIdDeleteErrors, RevokeOneTimeLinkOneTimeLinksLinkIdDeleteResponses, SharePasswordPasswordsPasswordIdSharePostData, SharePasswordPasswordsPasswordIdSharePostErrors, SharePasswordPasswordsPasswordIdSharePostResponses, SsoCallbackAuthSsoCallbackGetData, SsoCallbackAuthSsoCallbackGetErrors, SsoCallbackAuthSsoCallbackGetResponses, UnlockVaultVaultUnlockPostData, UnlockVaultVaultUnlockPostErrors, UnlockVaultVaultUnlockPostResponses, UnsharePasswordPasswordsPasswordIdShareGroupIdDeleteData, UnsharePasswordPasswordsPasswordIdShareGroupIdDeleteErrors, UnsharePasswordPasswordsPasswordIdShareGroupIdDeleteResponses, UpdateGroupGroupsGroupIdPutData, UpdateGroupGroupsGroupIdPutErrors, UpdateGroupGroupsGroupIdPutResponses, UpdatePasswordPasswordsPasswordIdPutData, UpdatePasswordPasswordsPasswordIdPutErrors, UpdatePasswordPasswordsPasswordIdPutResponses, UpdateShareExpirationPasswordsPasswordIdShareGroupIdPatchData, UpdateShareExpirationPasswordsPasswordIdShareGroupIdPatchErrors, UpdateShareExpirationPasswordsPasswordIdShareGroupIdPatchResponses, UpdateUserPasswordUsersMePasswordPutData, UpdateUserPasswordUsersMePasswordPutErrors, UpdateUserPasswordUsersMePasswordPutResponses, UpdateUserUsersUserIdPutData, UpdateUserUsersUserIdPutErrors, UpdateUserUsersUserIdPutResponses, ValidateVaultSetupVaultValidateSetupPostData, ValidateVaultSetupVaultValidateSetupPostErrors, ValidateVaultSetupVaultValidateSetupPostResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * Health Check
 */
export const healthCheckHealthGet = <ThrowOnError extends boolean = false>(options?: Options<HealthCheckHealthGetData, ThrowOnError>): RequestResult<HealthCheckHealthGetResponses, unknown, ThrowOnError> => (options?.client ?? client).get<HealthCheckHealthGetResponses, unknown, ThrowOnError>({ url: '/health', ...options });

/**
 * Readiness Check
 */
export const readinessCheckHealthReadyGet = <ThrowOnError extends boolean = false>(options?: Options<ReadinessCheckHealthReadyGetData, ThrowOnError>): RequestResult<ReadinessCheckHealthReadyGetResponses, unknown, ThrowOnError> => (options?.client ?? client).get<ReadinessCheckHealthReadyGetResponses, unknown, ThrowOnError>({ url: '/health/ready', ...options });

/**
 * Get CSRF token
 *
 * Get a CSRF token for the current authenticated user.
 *
 * This endpoint must be called after successful authentication to obtain
 * a CSRF token. The token must be included in the X-CSRF-Token header
 * for all mutating requests (POST, PUT, DELETE, PATCH).
 *
 * - **Authorization**: Requires valid access_token cookie
 *
 * Returns a CSRF token that remains valid for the entire session.
 */
export const getCsrfTokenAuthCsrfTokenGet = <ThrowOnError extends boolean = false>(options?: Options<GetCsrfTokenAuthCsrfTokenGetData, ThrowOnError>): RequestResult<GetCsrfTokenAuthCsrfTokenGetResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetCsrfTokenAuthCsrfTokenGetResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/auth/csrf-token',
    ...options
});

/**
 * Create a new vault in pending state
 *
 * Create a new vault with Shamir's Secret Sharing in pending state.
 *
 * - **nb_shares**: Total number of shares to generate
 * - **threshold**: Minimum number of shares needed to unlock the vault
 *
 * Returns shares and a setup_id for validation.
 */
export const createVaultVaultSetupPost = <ThrowOnError extends boolean = false>(options: Options<CreateVaultVaultSetupPostData, ThrowOnError>): RequestResult<CreateVaultVaultSetupPostResponses, CreateVaultVaultSetupPostErrors, ThrowOnError> => (options.client ?? client).post<CreateVaultVaultSetupPostResponses, CreateVaultVaultSetupPostErrors, ThrowOnError>({
    url: '/vault/setup',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Validate and complete vault setup
 *
 * Validate and complete vault setup using the setup_id from initial setup.
 *
 * - **setup_id**: The unique identifier returned from the initial setup
 */
export const validateVaultSetupVaultValidateSetupPost = <ThrowOnError extends boolean = false>(options: Options<ValidateVaultSetupVaultValidateSetupPostData, ThrowOnError>): RequestResult<ValidateVaultSetupVaultValidateSetupPostResponses, ValidateVaultSetupVaultValidateSetupPostErrors, ThrowOnError> => (options.client ?? client).post<ValidateVaultSetupVaultValidateSetupPostResponses, ValidateVaultSetupVaultValidateSetupPostErrors, ThrowOnError>({
    url: '/vault/validate-setup',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Unlock the vault
 *
 * Unlock the vault using Shamir's Secret Sharing reconstruction.
 *
 * This endpoint does not require authentication as it's needed to unlock the vault
 * before any user can authenticate.
 *
 * - **shares**: List of share secrets (hex strings with embedded index in format "index:hexsecret")
 *
 * Shares are added to any existing pending shares. Use DELETE /vault/unlock/clear
 * to remove all pending shares before submitting new ones.
 *
 * Returns:
 * - 200: Vault unlocked successfully
 * - 202: Shares accepted and stored, but more shares needed to unlock
 * - 400: Invalid shares or other domain error
 */
export const unlockVaultVaultUnlockPost = <ThrowOnError extends boolean = false>(options: Options<UnlockVaultVaultUnlockPostData, ThrowOnError>): RequestResult<UnlockVaultVaultUnlockPostResponses, UnlockVaultVaultUnlockPostErrors, ThrowOnError> => (options.client ?? client).post<UnlockVaultVaultUnlockPostResponses, UnlockVaultVaultUnlockPostErrors, ThrowOnError>({
    url: '/vault/unlock',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Clear all pending shares
 *
 * Clear all pending shares that were submitted but didn't unlock the vault.
 *
 * This endpoint allows users to reset the unlock process by removing all
 * previously submitted shares. Useful when shares are stale or incorrect.
 *
 * This endpoint does not require authentication as it's part of the unlock flow.
 */
export const clearPendingSharesVaultUnlockClearDelete = <ThrowOnError extends boolean = false>(options?: Options<ClearPendingSharesVaultUnlockClearDeleteData, ThrowOnError>): RequestResult<ClearPendingSharesVaultUnlockClearDeleteResponses, unknown, ThrowOnError> => (options?.client ?? client).delete<ClearPendingSharesVaultUnlockClearDeleteResponses, unknown, ThrowOnError>({ url: '/vault/unlock/clear', ...options });

/**
 * Lock the vault
 *
 * Lock the vault by clearing the decrypted key from memory.
 *
 * The vault must be unlocked to be locked again.
 * Only administrators can lock the vault.
 *
 * - **Authorization**: Requires authentication via access_token cookie
 */
export const lockVaultVaultLockPost = <ThrowOnError extends boolean = false>(options?: Options<LockVaultVaultLockPostData, ThrowOnError>): RequestResult<LockVaultVaultLockPostResponses, unknown, ThrowOnError> => (options?.client ?? client).post<LockVaultVaultLockPostResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/vault/lock',
    ...options
});

/**
 * Get the current status of the vault
 *
 * Retrieve the current status of the vault.
 *
 * This endpoint provides information about the vault's operational state:
 * NOT_SETUP, LOCKED, PENDING_UNLOCK, or UNLOCKED.
 *
 * When status is PENDING_UNLOCK, last_share_timestamp indicates when
 * the last share was submitted.
 */
export const getVaultStatusVaultStatusGet = <ThrowOnError extends boolean = false>(options?: Options<GetVaultStatusVaultStatusGetData, ThrowOnError>): RequestResult<GetVaultStatusVaultStatusGetResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetVaultStatusVaultStatusGetResponses, unknown, ThrowOnError>({ url: '/vault/status', ...options });

/**
 * Get password statistics for admin
 *
 * Retrieve password statistics.
 *
 * - **Authentication**: Requires authentication via access_token cookie
 * - **Authorization**: Only administrators can access this endpoint
 *
 * Returns the total count of passwords in the system, along with how many
 * one-time links have ever been issued and how many are still redeemable.
 */
export const getPasswordStatisticForAdminPasswordsStatisticsGet = <ThrowOnError extends boolean = false>(options?: Options<GetPasswordStatisticForAdminPasswordsStatisticsGetData, ThrowOnError>): RequestResult<GetPasswordStatisticForAdminPasswordsStatisticsGetResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetPasswordStatisticForAdminPasswordsStatisticsGetResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/passwords/statistics',
    ...options
});

/**
 * Create a new password
 *
 * Create a new password entry.
 *
 * - **name**: Name/title for the password entry
 * - **password**: The actual password to store (will be encrypted)
 * - **folder**: Optional folder to organize the password
 * - **login**: Optional login or username associated with the password
 * - **url**: Optional URL associated with the password entry
 * - **group_id**: Optional group ID. If not provided, uses the user's personal group
 * - **Authentication**: Requires authentication via access_token cookie
 */
export const createPasswordPasswordsPost = <ThrowOnError extends boolean = false>(options: Options<CreatePasswordPasswordsPostData, ThrowOnError>): RequestResult<CreatePasswordPasswordsPostResponses, CreatePasswordPasswordsPostErrors, ThrowOnError> => (options.client ?? client).post<CreatePasswordPasswordsPostResponses, CreatePasswordPasswordsPostErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/passwords/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List all passwords, optionally filtered by folder
 *
 * List all passwords for the authenticated user, optionally filtered by folder.
 *
 * - **folder**: Optional folder name to filter passwords
 * - **Authentication**: an access_token cookie, or a browser-extension bearer
 * token. An extension token is read-only and never carries the admin role,
 * so it reaches only what its own user can read.
 *
 * Returns a list of passwords accessible by the user.
 *
 * **access_expires_at** is set when the caller reaches the password through a
 * time-limited share; it is null when they own it or the share is permanent.
 */
export const listPasswordsPasswordsListGet = <ThrowOnError extends boolean = false>(options?: Options<ListPasswordsPasswordsListGetData, ThrowOnError>): RequestResult<ListPasswordsPasswordsListGetResponses, ListPasswordsPasswordsListGetErrors, ThrowOnError> => (options?.client ?? client).get<ListPasswordsPasswordsListGetResponses, ListPasswordsPasswordsListGetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }, { scheme: 'bearer', type: 'http' }],
    url: '/passwords/list',
    ...options
});

/**
 * List events for a password
 *
 * Retrieve the event history for a specific password.
 *
 * Admins can list events for any password. Owners and members can list events
 * for passwords they have access to through their groups.
 *
 * - **password_id**: The ID of the password
 * - **event_type**: Optional filter by event types
 * - **start_date**: Optional filter from this date
 * - **end_date**: Optional filter until this date
 * - **Authentication**: Requires authentication via access_token cookie
 */
export const listPasswordEventsPasswordsPasswordIdEventsGet = <ThrowOnError extends boolean = false>(options: Options<ListPasswordEventsPasswordsPasswordIdEventsGetData, ThrowOnError>): RequestResult<ListPasswordEventsPasswordsPasswordIdEventsGetResponses, ListPasswordEventsPasswordsPasswordIdEventsGetErrors, ThrowOnError> => (options.client ?? client).get<ListPasswordEventsPasswordsPasswordIdEventsGetResponses, ListPasswordEventsPasswordsPasswordIdEventsGetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/passwords/{password_id}/events',
    ...options
});

/**
 * List all password actions performed by a user (admin only)
 *
 * Retrieve the full action history (create, read, update, delete, share, unshare)
 * performed by a given user across **all** passwords.
 *
 * - **user_id**: The ID of the user whose actions are being audited
 * - **event_type**: Optional filter by event types
 * - **start_date**: Optional filter from this date
 * - **end_date**: Optional filter until this date
 * - **Authentication**: Requires authentication via access_token cookie
 * - **Authorization**: Only administrators can access this endpoint
 */
export const listPasswordEventsByActorAdminUsersUserIdPasswordEventsGet = <ThrowOnError extends boolean = false>(options: Options<ListPasswordEventsByActorAdminUsersUserIdPasswordEventsGetData, ThrowOnError>): RequestResult<ListPasswordEventsByActorAdminUsersUserIdPasswordEventsGetResponses, ListPasswordEventsByActorAdminUsersUserIdPasswordEventsGetErrors, ThrowOnError> => (options.client ?? client).get<ListPasswordEventsByActorAdminUsersUserIdPasswordEventsGetResponses, ListPasswordEventsByActorAdminUsersUserIdPasswordEventsGetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/admin/users/{user_id}/password-events',
    ...options
});

/**
 * List the one-time links issued for a password
 *
 * List the one-time links issued for a password.
 *
 * - **password_id**: UUID of the password
 * - **include_inactive**: also return spent, revoked and expired links
 * - **Authentication**: Requires authentication via access_token cookie (owner only)
 *
 * Returns only the still-redeemable links by default: those are the ones the
 * owner can revoke, and their number is bounded by the active-link cap. Passing
 * `include_inactive` returns the recent history too, capped, since spent links
 * are kept indefinitely. `active`, `max_active` and `total` describe the whole
 * set regardless of the filter. Tokens are never returned, not even hashed.
 */
export const listOneTimeLinksPasswordsPasswordIdOneTimeLinksGet = <ThrowOnError extends boolean = false>(options: Options<ListOneTimeLinksPasswordsPasswordIdOneTimeLinksGetData, ThrowOnError>): RequestResult<ListOneTimeLinksPasswordsPasswordIdOneTimeLinksGetResponses, ListOneTimeLinksPasswordsPasswordIdOneTimeLinksGetErrors, ThrowOnError> => (options.client ?? client).get<ListOneTimeLinksPasswordsPasswordIdOneTimeLinksGetResponses, ListOneTimeLinksPasswordsPasswordIdOneTimeLinksGetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/passwords/{password_id}/one-time-links',
    ...options
});

/**
 * Generate a one-time link for a password
 *
 * Generate a single-use link granting anonymous read access to one password.
 *
 * - **password_id**: UUID of the password to share
 * - **lifetime_seconds**: optional validity window, 24h by default
 * - **Authentication**: Requires authentication via access_token cookie (owner only)
 *
 * The token is returned here and never again: only its hash is stored.
 *
 * Answers 409 when the password already has the maximum number of links that
 * are still redeemable. Read, revoked and expired links do not count.
 */
export const createOneTimeLinkPasswordsPasswordIdOneTimeLinksPost = <ThrowOnError extends boolean = false>(options: Options<CreateOneTimeLinkPasswordsPasswordIdOneTimeLinksPostData, ThrowOnError>): RequestResult<CreateOneTimeLinkPasswordsPasswordIdOneTimeLinksPostResponses, CreateOneTimeLinkPasswordsPasswordIdOneTimeLinksPostErrors, ThrowOnError> => (options.client ?? client).post<CreateOneTimeLinkPasswordsPasswordIdOneTimeLinksPostResponses, CreateOneTimeLinkPasswordsPasswordIdOneTimeLinksPostErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/passwords/{password_id}/one-time-links',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the one-time links the caller issued
 *
 * List every one-time link the calling user issued, across all passwords.
 *
 * - **include_inactive**: also return spent, revoked and expired links
 * - **Authentication**: Requires authentication via access_token cookie
 *
 * Returns only the still-redeemable links by default. Tokens are never returned.
 */
export const listMyOneTimeLinksOneTimeLinksMineGet = <ThrowOnError extends boolean = false>(options?: Options<ListMyOneTimeLinksOneTimeLinksMineGetData, ThrowOnError>): RequestResult<ListMyOneTimeLinksOneTimeLinksMineGetResponses, ListMyOneTimeLinksOneTimeLinksMineGetErrors, ThrowOnError> => (options?.client ?? client).get<ListMyOneTimeLinksOneTimeLinksMineGetResponses, ListMyOneTimeLinksOneTimeLinksMineGetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/one-time-links/mine',
    ...options
});

/**
 * List every one-time link in the vault (admin only)
 *
 * List the one-time links issued anywhere in the vault.
 *
 * - **include_inactive**: also return spent, revoked and expired links
 * - **Authentication**: Requires authentication via access_token cookie
 * - **Authorization**: Only administrators can access this endpoint
 *
 * Every active link is an outstanding anonymous read grant, so this is the view
 * that says how much of that access is currently open. Tokens are never returned.
 */
export const listOneTimeLinksForAdminAdminOneTimeLinksGet = <ThrowOnError extends boolean = false>(options?: Options<ListOneTimeLinksForAdminAdminOneTimeLinksGetData, ThrowOnError>): RequestResult<ListOneTimeLinksForAdminAdminOneTimeLinksGetResponses, ListOneTimeLinksForAdminAdminOneTimeLinksGetErrors, ThrowOnError> => (options?.client ?? client).get<ListOneTimeLinksForAdminAdminOneTimeLinksGetResponses, ListOneTimeLinksForAdminAdminOneTimeLinksGetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/admin/one-time-links',
    ...options
});

/**
 * Revoke any one-time link (admin only)
 *
 * Revoke a one-time link regardless of who issued it.
 *
 * - **link_id**: UUID of the link to revoke
 * - **Authentication**: Requires authentication via access_token cookie
 * - **Authorization**: Only administrators can access this endpoint
 *
 * Links that were already read cannot be revoked: their read timestamp is audit data.
 */
export const revokeOneTimeLinkForAdminAdminOneTimeLinksLinkIdDelete = <ThrowOnError extends boolean = false>(options: Options<RevokeOneTimeLinkForAdminAdminOneTimeLinksLinkIdDeleteData, ThrowOnError>): RequestResult<RevokeOneTimeLinkForAdminAdminOneTimeLinksLinkIdDeleteResponses, RevokeOneTimeLinkForAdminAdminOneTimeLinksLinkIdDeleteErrors, ThrowOnError> => (options.client ?? client).delete<RevokeOneTimeLinkForAdminAdminOneTimeLinksLinkIdDeleteResponses, RevokeOneTimeLinkForAdminAdminOneTimeLinksLinkIdDeleteErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/admin/one-time-links/{link_id}',
    ...options
});

/**
 * Revoke every live one-time link a user issued (admin only)
 *
 * Revoke every still-redeemable link issued by one user, in a single call.
 *
 * - **user_id**: UUID of the user whose links are being cut
 * - **Authentication**: Requires authentication via access_token cookie
 * - **Authorization**: Only administrators can access this endpoint
 *
 * Meant for the moment someone loses their access: the links they handed out
 * would otherwise outlive their account. Already-read links are left untouched.
 */
export const revokeAllOneTimeLinksForUserAdminUsersUserIdOneTimeLinksDelete = <ThrowOnError extends boolean = false>(options: Options<RevokeAllOneTimeLinksForUserAdminUsersUserIdOneTimeLinksDeleteData, ThrowOnError>): RequestResult<RevokeAllOneTimeLinksForUserAdminUsersUserIdOneTimeLinksDeleteResponses, RevokeAllOneTimeLinksForUserAdminUsersUserIdOneTimeLinksDeleteErrors, ThrowOnError> => (options.client ?? client).delete<RevokeAllOneTimeLinksForUserAdminUsersUserIdOneTimeLinksDeleteResponses, RevokeAllOneTimeLinksForUserAdminUsersUserIdOneTimeLinksDeleteErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/admin/users/{user_id}/one-time-links',
    ...options
});

/**
 * Revoke a one-time link
 *
 * Revoke a one-time link that has not been read yet.
 *
 * - **link_id**: UUID of the link to revoke
 * - **Authentication**: Requires authentication via access_token cookie (owner only)
 *
 * Links that were already read cannot be revoked: their read timestamp is audit data.
 */
export const revokeOneTimeLinkOneTimeLinksLinkIdDelete = <ThrowOnError extends boolean = false>(options: Options<RevokeOneTimeLinkOneTimeLinksLinkIdDeleteData, ThrowOnError>): RequestResult<RevokeOneTimeLinkOneTimeLinksLinkIdDeleteResponses, RevokeOneTimeLinkOneTimeLinksLinkIdDeleteErrors, ThrowOnError> => (options.client ?? client).delete<RevokeOneTimeLinkOneTimeLinksLinkIdDeleteResponses, RevokeOneTimeLinkOneTimeLinksLinkIdDeleteErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/one-time-links/{link_id}',
    ...options
});

/**
 * Redeem a one-time link
 *
 * Redeem a one-time link and return the secret it points at.
 *
 * - **token**: the secret carried by the link
 * - **Authentication**: intentionally anonymous. The token in the body is the
 * only credential; recipients are outside the vault by design.
 *
 * The token is sent in the body rather than the URL so it stays out of server
 * logs and Referer headers, and this is a POST so that link scanners and mail
 * previewers cannot burn the link by merely following the URL.
 *
 * A locked vault answers 503 without consuming the link, so the recipient can
 * retry once it is unlocked.
 */
export const consumeOneTimeLinkOneTimeLinksConsumePost = <ThrowOnError extends boolean = false>(options: Options<ConsumeOneTimeLinkOneTimeLinksConsumePostData, ThrowOnError>): RequestResult<ConsumeOneTimeLinkOneTimeLinksConsumePostResponses, ConsumeOneTimeLinkOneTimeLinksConsumePostErrors, ThrowOnError> => (options.client ?? client).post<ConsumeOneTimeLinkOneTimeLinksConsumePostResponses, ConsumeOneTimeLinkOneTimeLinksConsumePostErrors, ThrowOnError>({
    url: '/one-time-links/consume',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a password
 *
 * Delete a password by its ID.
 *
 * - **password_id**: ID of the password to delete
 * - **Authentication**: Requires authentication via access_token cookie
 *
 * Returns status code 204 (No Content) on successful deletion.
 */
export const deletePasswordPasswordsPasswordIdDelete = <ThrowOnError extends boolean = false>(options: Options<DeletePasswordPasswordsPasswordIdDeleteData, ThrowOnError>): RequestResult<DeletePasswordPasswordsPasswordIdDeleteResponses, DeletePasswordPasswordsPasswordIdDeleteErrors, ThrowOnError> => (options.client ?? client).delete<DeletePasswordPasswordsPasswordIdDeleteResponses, DeletePasswordPasswordsPasswordIdDeleteErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/passwords/{password_id}',
    ...options
});

/**
 * Get a password by ID
 *
 * Retrieve a password by its ID with user authentication.
 *
 * - **password_id**: The ID of the password to retrieve
 * - **Authentication**: an access_token cookie, or a browser-extension bearer
 * token. An extension token is read-only and never carries the admin role,
 * so it reaches only what its own user can read.
 */
export const getPasswordPasswordsPasswordIdGet = <ThrowOnError extends boolean = false>(options: Options<GetPasswordPasswordsPasswordIdGetData, ThrowOnError>): RequestResult<GetPasswordPasswordsPasswordIdGetResponses, GetPasswordPasswordsPasswordIdGetErrors, ThrowOnError> => (options.client ?? client).get<GetPasswordPasswordsPasswordIdGetResponses, GetPasswordPasswordsPasswordIdGetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }, { scheme: 'bearer', type: 'http' }],
    url: '/passwords/{password_id}',
    ...options
});

/**
 * Update an existing password
 *
 * Update an existing password by its ID.
 *
 * - **password_id**: ID of the resource
 * - **name**: Name/title for the password entry
 * - **password**: The actual password to store (will be encrypted)
 * - **folder**: Optional folder to organize the password
 * - **login**: Optional login/username associated with the password entry
 * - **url**: Optional URL associated with the password entry
 * - **Authentication**: Requires authentication via access_token cookie
 */
export const updatePasswordPasswordsPasswordIdPut = <ThrowOnError extends boolean = false>(options: Options<UpdatePasswordPasswordsPasswordIdPutData, ThrowOnError>): RequestResult<UpdatePasswordPasswordsPasswordIdPutResponses, UpdatePasswordPasswordsPasswordIdPutErrors, ThrowOnError> => (options.client ?? client).put<UpdatePasswordPasswordsPasswordIdPutResponses, UpdatePasswordPasswordsPasswordIdPutErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/passwords/{password_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Share a password with a group
 *
 * Share a password with a group, permanently or for a limited time.
 *
 * - **password_id**: UUID of the password to share
 * - **group_id**: UUID of the group to grant access to
 * - **expires_at**: optional date the access lapses on; omit it to share permanently
 * - **Authentication**: Requires authentication via access_token cookie (owner only)
 *
 * Returns status code 201 on successful sharing.
 */
export const sharePasswordPasswordsPasswordIdSharePost = <ThrowOnError extends boolean = false>(options: Options<SharePasswordPasswordsPasswordIdSharePostData, ThrowOnError>): RequestResult<SharePasswordPasswordsPasswordIdSharePostResponses, SharePasswordPasswordsPasswordIdSharePostErrors, ThrowOnError> => (options.client ?? client).post<SharePasswordPasswordsPasswordIdSharePostResponses, SharePasswordPasswordsPasswordIdSharePostErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/passwords/{password_id}/share',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke password access from a group
 *
 * Remove sharing of a password from a specific group.
 *
 * - **password_id**: UUID of the password to unshare
 * - **group_id**: UUID of the group to revoke access from
 * - **Authentication**: Requires authentication via access_token cookie (owner only)
 *
 * Returns status code 204 (No Content) on successful unsharing.
 *
 * Note: Cannot unshare with the owner group of the password.
 */
export const unsharePasswordPasswordsPasswordIdShareGroupIdDelete = <ThrowOnError extends boolean = false>(options: Options<UnsharePasswordPasswordsPasswordIdShareGroupIdDeleteData, ThrowOnError>): RequestResult<UnsharePasswordPasswordsPasswordIdShareGroupIdDeleteResponses, UnsharePasswordPasswordsPasswordIdShareGroupIdDeleteErrors, ThrowOnError> => (options.client ?? client).delete<UnsharePasswordPasswordsPasswordIdShareGroupIdDeleteResponses, UnsharePasswordPasswordsPasswordIdShareGroupIdDeleteErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/passwords/{password_id}/share/{group_id}',
    ...options
});

/**
 * Change when a password share expires
 *
 * Extend, shorten, or lift the deadline on an existing password share.
 *
 * - **password_id**: UUID of the shared password
 * - **group_id**: UUID of the group whose access is being retimed
 * - **expires_at**: new date the access lapses on; null makes the share permanent
 * - **Authentication**: Requires authentication via access_token cookie (owner only)
 *
 * A share that has already expired can still be extended, as long as it has not
 * been purged yet.
 *
 * Returns status code 204 (No Content) on success.
 */
export const updateShareExpirationPasswordsPasswordIdShareGroupIdPatch = <ThrowOnError extends boolean = false>(options: Options<UpdateShareExpirationPasswordsPasswordIdShareGroupIdPatchData, ThrowOnError>): RequestResult<UpdateShareExpirationPasswordsPasswordIdShareGroupIdPatchResponses, UpdateShareExpirationPasswordsPasswordIdShareGroupIdPatchErrors, ThrowOnError> => (options.client ?? client).patch<UpdateShareExpirationPasswordsPasswordIdShareGroupIdPatchResponses, UpdateShareExpirationPasswordsPasswordIdShareGroupIdPatchErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/passwords/{password_id}/share/{group_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List all users who have access to a password
 *
 * List all users who have access to a specific password.
 *
 * - **password_id**: UUID of the password to query access for
 * - **Authentication**: Requires authentication via access_token cookie (owner only)
 *
 * Returns a list of users with their permissions and ownership status.
 * Only the owner of the password can list who has access to it.
 *
 * Shares that have already expired are still listed, carrying a past
 * **expires_at**, so their owner can see them and extend them.
 */
export const listPasswordAccessPasswordsPasswordIdAccessGet = <ThrowOnError extends boolean = false>(options: Options<ListPasswordAccessPasswordsPasswordIdAccessGetData, ThrowOnError>): RequestResult<ListPasswordAccessPasswordsPasswordIdAccessGetResponses, ListPasswordAccessPasswordsPasswordIdAccessGetErrors, ThrowOnError> => (options.client ?? client).get<ListPasswordAccessPasswordsPasswordIdAccessGetResponses, ListPasswordAccessPasswordsPasswordIdAccessGetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/passwords/{password_id}/access',
    ...options
});

/**
 * Get current user information
 *
 * Retrieve the authenticated user's information including personal group ID.
 *
 * - **Authentication**: Requires authentication via access_token cookie
 *
 * Returns the current user's profile including id, username, email, name, roles, personal_group_id, and is_sso.
 */
export const getUserMeUsersMeGet = <ThrowOnError extends boolean = false>(options?: Options<GetUserMeUsersMeGetData, ThrowOnError>): RequestResult<GetUserMeUsersMeGetResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetUserMeUsersMeGetResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/users/me',
    ...options
});

/**
 * Update current user's password
 *
 * Update the authenticated user's password.
 *
 * - **old_password**: Current password for verification
 * - **new_password**: New password to set
 * - **Authentication**: Requires authentication via access_token cookie
 *
 * Returns status code 204 (No Content) on successful password update.
 *
 * Raises:
 * - 401: If old password is incorrect
 * - 404: If user not found
 */
export const updateUserPasswordUsersMePasswordPut = <ThrowOnError extends boolean = false>(options: Options<UpdateUserPasswordUsersMePasswordPutData, ThrowOnError>): RequestResult<UpdateUserPasswordUsersMePasswordPutResponses, UpdateUserPasswordUsersMePasswordPutErrors, ThrowOnError> => (options.client ?? client).put<UpdateUserPasswordUsersMePasswordPutResponses, UpdateUserPasswordUsersMePasswordPutErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/users/me/password',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a user by ID
 *
 * Delete a user by its ID.
 *
 * - **Authentication**: Requires authentication via access_token cookie
 *
 * Returns status code 204 (No Content) on successful deletion.
 */
export const deleteUserUsersUserIdDelete = <ThrowOnError extends boolean = false>(options: Options<DeleteUserUsersUserIdDeleteData, ThrowOnError>): RequestResult<DeleteUserUsersUserIdDeleteResponses, DeleteUserUsersUserIdDeleteErrors, ThrowOnError> => (options.client ?? client).delete<DeleteUserUsersUserIdDeleteResponses, DeleteUserUsersUserIdDeleteErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/users/{user_id}',
    ...options
});

/**
 * Get a user by ID
 *
 * Retrieve a user by its ID.
 *
 * - **user_id**: The ID of the user to retrieve
 *
 * - **Authentication**: Requires authentication via access_token cookie
 */
export const getUserUsersUserIdGet = <ThrowOnError extends boolean = false>(options: Options<GetUserUsersUserIdGetData, ThrowOnError>): RequestResult<GetUserUsersUserIdGetResponses, GetUserUsersUserIdGetErrors, ThrowOnError> => (options.client ?? client).get<GetUserUsersUserIdGetResponses, GetUserUsersUserIdGetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/users/{user_id}',
    ...options
});

/**
 * Update a user by ID
 *
 * Update a user by its ID.
 *
 * - **user_id**: The ID of the user to update
 * - **username**: New username for the user
 * - **email**: New email for the user
 * - **name**: New display name for the user
 *
 * - **Authentication**: Requires authentication via access_token cookie
 * - **Authorization**: Only the user themselves or an administrator may update a user
 *
 * Returns the updated user ID.
 */
export const updateUserUsersUserIdPut = <ThrowOnError extends boolean = false>(options: Options<UpdateUserUsersUserIdPutData, ThrowOnError>): RequestResult<UpdateUserUsersUserIdPutResponses, UpdateUserUsersUserIdPutErrors, ThrowOnError> => (options.client ?? client).put<UpdateUserUsersUserIdPutResponses, UpdateUserUsersUserIdPutErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/users/{user_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List all users
 *
 * Retrieve all users.
 *
 * - **Authentication**: Requires authentication via access_token cookie
 * - **Authorization**: Only administrators may list users
 *
 * Returns a list of all users in the system.
 */
export const listUsersUsersGet = <ThrowOnError extends boolean = false>(options?: Options<ListUsersUsersGetData, ThrowOnError>): RequestResult<ListUsersUsersGetResponses, unknown, ThrowOnError> => (options?.client ?? client).get<ListUsersUsersGetResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/users/',
    ...options
});

/**
 * Create a new user
 *
 * Create a new user with password authentication.
 *
 * - **username**: Unique username for the user
 * - **email**: User's email address
 * - **name**: User's display name
 * - **password**: Password for the user account
 * - **Authentication**: Requires authentication via access_token cookie
 * - **Authorization**: Admin only
 */
export const createUserUsersPost = <ThrowOnError extends boolean = false>(options: Options<CreateUserUsersPostData, ThrowOnError>): RequestResult<CreateUserUsersPostResponses, CreateUserUsersPostErrors, ThrowOnError> => (options.client ?? client).post<CreateUserUsersPostResponses, CreateUserUsersPostErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/users/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Promote a user to admin
 *
 * Promote a user to administrator role.
 *
 * - **user_id**: UUID of the user to promote
 * - **Authentication**: Requires authentication via access_token cookie
 * - **Authorization**: Only administrators can promote users
 *
 * Returns status code 204 (No Content) on successful promotion.
 *
 * Raises:
 * - 403: If requesting user is not an admin
 * - 404: If target user does not exist
 * - 400: If user is already an admin
 */
export const promoteUserToAdminUsersUserIdPromoteAdminPost = <ThrowOnError extends boolean = false>(options: Options<PromoteUserToAdminUsersUserIdPromoteAdminPostData, ThrowOnError>): RequestResult<PromoteUserToAdminUsersUserIdPromoteAdminPostResponses, PromoteUserToAdminUsersUserIdPromoteAdminPostErrors, ThrowOnError> => (options.client ?? client).post<PromoteUserToAdminUsersUserIdPromoteAdminPostResponses, PromoteUserToAdminUsersUserIdPromoteAdminPostErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/users/{user_id}/promote-admin',
    ...options
});

/**
 * Login admin user
 *
 * Login an admin user.
 *
 * - **email**: Email address of the admin user
 * - **password**: Password of the admin user
 *
 * Returns admin information and sets an HTTP-only secure cookie with the JWT token.
 */
export const adminLoginAuthLoginPost = <ThrowOnError extends boolean = false>(options: Options<AdminLoginAuthLoginPostData, ThrowOnError>): RequestResult<AdminLoginAuthLoginPostResponses, AdminLoginAuthLoginPostErrors, ThrowOnError> => (options.client ?? client).post<AdminLoginAuthLoginPostResponses, AdminLoginAuthLoginPostErrors, ThrowOnError>({
    url: '/auth/login',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Register the first admin user
 *
 * Register the first admin user for the system.
 *
 * - **email**: Email address for the admin user
 * - **password**: Password for the admin user (will be hashed)
 * - **display_name**: Display name for the admin user
 *
 * Returns the created admin information with generated ID.
 */
export const registerAdminAuthRegisterAdminPost = <ThrowOnError extends boolean = false>(options: Options<RegisterAdminAuthRegisterAdminPostData, ThrowOnError>): RequestResult<RegisterAdminAuthRegisterAdminPostResponses, RegisterAdminAuthRegisterAdminPostErrors, ThrowOnError> => (options.client ?? client).post<RegisterAdminAuthRegisterAdminPostResponses, RegisterAdminAuthRegisterAdminPostErrors, ThrowOnError>({
    url: '/auth/register-admin',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Configure SSO provider with OpenID Connect Discovery
 *
 * Configure an SSO provider via OpenID Connect auto-discovery.
 *
 * **Simplified configuration with only 3 parameters:**
 *
 * ```json
 * {
 * "client_id": "your-client-id",
 * "client_secret": "your-client-secret",
 * "discovery_url": "https://provider.com/.well-known/openid_configuration"
 * }
 * ```
 *
 * **Example discovery URLs for popular providers:**
 *
 * - **Google**: `https://accounts.google.com/.well-known/openid_configuration`
 * - **Microsoft**: `https://login.microsoftonline.com/{tenant}/.well-known/openid_configuration`
 * - **Keycloak**: `https://keycloak.example.com/realms/{realm}/.well-known/openid_configuration`
 * - **Auth0**: `https://{domain}.auth0.com/.well-known/openid_configuration`
 * - **Okta**: `https://{domain}.okta.com/.well-known/openid_configuration`
 *
 * Auto-discovery retrieves all necessary endpoints from the OpenID Connect configuration URL,
 * ensuring compatibility with the standard.
 */
export const configureSsoProviderAuthSsoConfigurePost = <ThrowOnError extends boolean = false>(options: Options<ConfigureSsoProviderAuthSsoConfigurePostData, ThrowOnError>): RequestResult<ConfigureSsoProviderAuthSsoConfigurePostResponses, ConfigureSsoProviderAuthSsoConfigurePostErrors, ThrowOnError> => (options.client ?? client).post<ConfigureSsoProviderAuthSsoConfigurePostResponses, ConfigureSsoProviderAuthSsoConfigurePostErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/auth/sso/configure',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get SSO authorization URL
 *
 * Get the SSO authorization URL.
 *
 * Generates a CSRF ``state`` bound to the caller via an httpOnly cookie,
 * embeds it in the authorization URL, and returns that URL. The state is
 * verified against the cookie when the provider calls back.
 */
export const getSsoUrlAuthSsoUrlGet = <ThrowOnError extends boolean = false>(options?: Options<GetSsoUrlAuthSsoUrlGetData, ThrowOnError>): RequestResult<GetSsoUrlAuthSsoUrlGetResponses, GetSsoUrlAuthSsoUrlGetErrors, ThrowOnError> => (options?.client ?? client).get<GetSsoUrlAuthSsoUrlGetResponses, GetSsoUrlAuthSsoUrlGetErrors, ThrowOnError>({ url: '/auth/sso/url', ...options });

/**
 * SSO callback endpoint
 *
 * SSO callback endpoint.
 *
 * This endpoint is called by the SSO provider after the user has authorized the application.
 * It exchanges the authorization code for an access token and signs the user in.
 *
 * - **code**: The authorization code provided by the SSO provider
 * - **state**: (Optional) State parameter for CSRF protection
 * - **redirect_uri**: (Optional) Redirect URI used during authorization, required for CLI auth flows
 *
 * Returns user information and sets HTTP-only secure cookies with JWT tokens.
 */
export const ssoCallbackAuthSsoCallbackGet = <ThrowOnError extends boolean = false>(options: Options<SsoCallbackAuthSsoCallbackGetData, ThrowOnError>): RequestResult<SsoCallbackAuthSsoCallbackGetResponses, SsoCallbackAuthSsoCallbackGetErrors, ThrowOnError> => (options.client ?? client).get<SsoCallbackAuthSsoCallbackGetResponses, SsoCallbackAuthSsoCallbackGetErrors, ThrowOnError>({ url: '/auth/sso/callback', ...options });

/**
 * Check if SSO is configured
 *
 * Check whether SSO (Single Sign-On) has been configured.
 *
 * Intentionally anonymous: the login page calls this before authentication to
 * show or hide the SSO button. Returns only ``is_set``, never any provider
 * detail.
 */
export const isSsoConfigSetAuthSsoIsConfiguredGet = <ThrowOnError extends boolean = false>(options?: Options<IsSsoConfigSetAuthSsoIsConfiguredGetData, ThrowOnError>): RequestResult<IsSsoConfigSetAuthSsoIsConfiguredGetResponses, unknown, ThrowOnError> => (options?.client ?? client).get<IsSsoConfigSetAuthSsoIsConfiguredGetResponses, unknown, ThrowOnError>({ url: '/auth/sso/is-configured', ...options });

/**
 * Refresh access token
 *
 * Refresh access token using a refresh token.
 *
 * This endpoint allows clients to obtain a new access token using a valid refresh token.
 * The refresh token must be provided as an HTTP-only cookie.
 *
 * Sets a new access token and a rotated refresh token in HTTP-only secure cookies.
 *
 * **Responses**:
 * - **200**: Successfully refreshed access token
 * - **400**: Invalid or expired refresh token, or user no longer exists
 * - **500**: Internal server error
 */
export const refreshAccessTokenAuthRefreshTokenPost = <ThrowOnError extends boolean = false>(options?: Options<RefreshAccessTokenAuthRefreshTokenPostData, ThrowOnError>): RequestResult<RefreshAccessTokenAuthRefreshTokenPostResponses, RefreshAccessTokenAuthRefreshTokenPostErrors, ThrowOnError> => (options?.client ?? client).post<RefreshAccessTokenAuthRefreshTokenPostResponses, RefreshAccessTokenAuthRefreshTokenPostErrors, ThrowOnError>({ url: '/auth/refresh-token', ...options });

/**
 * Logout current session
 *
 * Logout the current browser session.
 *
 * - **access_token**: Optional access token cookie to revoke (may be expired/missing)
 * - **refresh_token**: Required refresh token cookie to revoke
 * - **Authentication**: Always clears cookies after revocation attempt
 */
export const logoutAuthLogoutPost = <ThrowOnError extends boolean = false>(options: Options<LogoutAuthLogoutPostData, ThrowOnError>): RequestResult<LogoutAuthLogoutPostResponses, LogoutAuthLogoutPostErrors, ThrowOnError> => (options.client ?? client).post<LogoutAuthLogoutPostResponses, LogoutAuthLogoutPostErrors, ThrowOnError>({ url: '/auth/logout', ...options });

/**
 * Create a new group
 *
 * Create a new group with the authenticated user as owner.
 *
 * - **name**: Name of the group to create
 * - **Authorization**: Bearer token required (access_token cookie)
 *
 * The authenticated user will be automatically set as the group owner.
 * Returns the created group information with generated ID.
 */
export const createGroupGroupsPost = <ThrowOnError extends boolean = false>(options: Options<CreateGroupGroupsPostData, ThrowOnError>): RequestResult<CreateGroupGroupsPostResponses, CreateGroupGroupsPostErrors, ThrowOnError> => (options.client ?? client).post<CreateGroupGroupsPostResponses, CreateGroupGroupsPostErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/groups/',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Add a member to a group
 *
 * Add a user as a member to a group.
 *
 * - **group_id**: ID of the group (path parameter)
 * - **user_id**: ID of the user to add as a member
 * - **Authorization**: Bearer token required (access_token cookie)
 * - **Permission**: Only group owners can add members
 *
 * The user will be added as a regular member (not an owner).
 * Cannot add members to personal groups.
 * Operation is idempotent - adding an existing member has no effect.
 */
export const addMemberToGroupGroupsGroupIdMembersPost = <ThrowOnError extends boolean = false>(options: Options<AddMemberToGroupGroupsGroupIdMembersPostData, ThrowOnError>): RequestResult<AddMemberToGroupGroupsGroupIdMembersPostResponses, AddMemberToGroupGroupsGroupIdMembersPostErrors, ThrowOnError> => (options.client ?? client).post<AddMemberToGroupGroupsGroupIdMembersPostResponses, AddMemberToGroupGroupsGroupIdMembersPostErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/groups/{group_id}/members',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Add an owner to a group
 *
 * Promote an existing member to owner of a group.
 *
 * - **group_id**: ID of the group (path parameter)
 * - **user_id**: ID of the user to promote to owner
 * - **Authorization**: Bearer token required (access_token cookie)
 * - **Permission**: Only group owners can add new owners
 *
 * The user must already be a member of the group before being promoted to owner.
 * Cannot add owners to personal groups.
 * Operation is idempotent - promoting an existing owner has no effect.
 */
export const addOwnerToGroupGroupsGroupIdOwnersPost = <ThrowOnError extends boolean = false>(options: Options<AddOwnerToGroupGroupsGroupIdOwnersPostData, ThrowOnError>): RequestResult<AddOwnerToGroupGroupsGroupIdOwnersPostResponses, AddOwnerToGroupGroupsGroupIdOwnersPostErrors, ThrowOnError> => (options.client ?? client).post<AddOwnerToGroupGroupsGroupIdOwnersPostResponses, AddOwnerToGroupGroupsGroupIdOwnersPostErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/groups/{group_id}/owners',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove a member from a group
 *
 * Remove a member from a group.
 *
 * - **group_id**: ID of the group (path parameter)
 * - **user_id**: ID of the user to remove (path parameter)
 * - **Authorization**: Bearer token required (access_token cookie)
 * - **Permission**: Only group owners can remove members
 *
 * Cannot remove owners from groups.
 * Cannot modify personal groups.
 * The user to be removed must be a member of the group.
 */
export const removeMemberFromGroupGroupsGroupIdMembersUserIdDelete = <ThrowOnError extends boolean = false>(options: Options<RemoveMemberFromGroupGroupsGroupIdMembersUserIdDeleteData, ThrowOnError>): RequestResult<RemoveMemberFromGroupGroupsGroupIdMembersUserIdDeleteResponses, RemoveMemberFromGroupGroupsGroupIdMembersUserIdDeleteErrors, ThrowOnError> => (options.client ?? client).delete<RemoveMemberFromGroupGroupsGroupIdMembersUserIdDeleteResponses, RemoveMemberFromGroupGroupsGroupIdMembersUserIdDeleteErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/groups/{group_id}/members/{user_id}',
    ...options
});

/**
 * Delete a group
 *
 * Delete a group by ID.
 *
 * - **group_id**: UUID of the group to delete
 * - **Authorization**: Bearer token required (access_token cookie)
 *
 * Only admins or group owners can delete a group.
 * Personal groups cannot be deleted.
 * Groups that are still in use (have passwords) cannot be deleted.
 */
export const deleteGroupGroupsGroupIdDelete = <ThrowOnError extends boolean = false>(options: Options<DeleteGroupGroupsGroupIdDeleteData, ThrowOnError>): RequestResult<DeleteGroupGroupsGroupIdDeleteResponses, DeleteGroupGroupsGroupIdDeleteErrors, ThrowOnError> => (options.client ?? client).delete<DeleteGroupGroupsGroupIdDeleteResponses, DeleteGroupGroupsGroupIdDeleteErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/groups/{group_id}',
    ...options
});

/**
 * Get a group by ID
 *
 * Retrieve a group by its ID.
 *
 * - **group_id**: UUID of the group to retrieve
 * - **Authorization**: Bearer token required (access_token cookie)
 *
 * Returns the group details including:
 * - id: The group's unique identifier
 * - name: The group's name
 * - is_personal: Whether this is a personal group
 * - user_id: The owner user ID (for personal groups) or null (for shared groups)
 * - owners: List of user IDs who are owners of the group
 * - members: List of user IDs who are members of the group
 */
export const getGroupGroupsGroupIdGet = <ThrowOnError extends boolean = false>(options: Options<GetGroupGroupsGroupIdGetData, ThrowOnError>): RequestResult<GetGroupGroupsGroupIdGetResponses, GetGroupGroupsGroupIdGetErrors, ThrowOnError> => (options.client ?? client).get<GetGroupGroupsGroupIdGetResponses, GetGroupGroupsGroupIdGetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/groups/{group_id}',
    ...options
});

/**
 * Update a group
 *
 * Update a group's name.
 *
 * - **group_id**: ID of the group to update (path parameter)
 * - **name**: New name for the group
 * - **Authorization**: Bearer token required (access_token cookie)
 * - **Permission**: Only group owners can update groups
 *
 * Cannot update personal groups.
 * Returns the updated group information.
 */
export const updateGroupGroupsGroupIdPut = <ThrowOnError extends boolean = false>(options: Options<UpdateGroupGroupsGroupIdPutData, ThrowOnError>): RequestResult<UpdateGroupGroupsGroupIdPutResponses, UpdateGroupGroupsGroupIdPutErrors, ThrowOnError> => (options.client ?? client).put<UpdateGroupGroupsGroupIdPutResponses, UpdateGroupGroupsGroupIdPutErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/groups/{group_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List all groups
 *
 * List all groups with optional filtering.
 *
 * - **include_personal**: Whether to include personal groups in the results (default: true)
 * - **Authorization**: Bearer token required (access_token cookie)
 *
 * Returns a list of all groups (or only shared groups if include_personal=false).
 * Each group includes:
 * - id: The group's unique identifier
 * - name: The group's name
 * - is_personal: Whether this is a personal group
 * - user_id: The owner user ID (for personal groups) or null (for shared groups)
 * - owners: List of user IDs who are owners of this group
 */
export const listGroupsGroupsGet = <ThrowOnError extends boolean = false>(options?: Options<ListGroupsGroupsGetData, ThrowOnError>): RequestResult<ListGroupsGroupsGetResponses, ListGroupsGroupsGetErrors, ThrowOnError> => (options?.client ?? client).get<ListGroupsGroupsGetResponses, ListGroupsGroupsGetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/groups',
    ...options
});

/**
 * Get statistics for admin
 *
 * Retrieve global statistics (number of users and groups).
 *
 * - **Authentication**: Requires authentication via access_token cookie
 * - **Authorization**: Only administrators can access this endpoint
 *
 * Returns the total count of users and groups in the system.
 */
export const getStatisticForAdminIamStatisticsGet = <ThrowOnError extends boolean = false>(options?: Options<GetStatisticForAdminIamStatisticsGetData, ThrowOnError>): RequestResult<GetStatisticForAdminIamStatisticsGetResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetStatisticForAdminIamStatisticsGetResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/iam/statistics',
    ...options
});

/**
 * Register a browser-extension pairing request
 *
 * Register a pairing request before the extension opens the approval tab.
 *
 * - **code_challenge**: base64url(SHA-256(code_verifier)); the verifier stays in the extension
 * - **code_challenge_method**: must be `S256`, `plain` is rejected
 * - **device_name**: shown on the approval page, explicitly labelled as self-reported
 * - **Authentication**: none required
 *
 * The pairing this creates grants nothing on its own: approving it needs a logged in
 * session, and redeeming it additionally needs the verifier. The returned `user_code` is
 * what the user matches against the code shown in their extension.
 */
export const registerExtensionDeviceExtensionDevicePost = <ThrowOnError extends boolean = false>(options: Options<RegisterExtensionDeviceExtensionDevicePostData, ThrowOnError>): RequestResult<RegisterExtensionDeviceExtensionDevicePostResponses, RegisterExtensionDeviceExtensionDevicePostErrors, ThrowOnError> => (options.client ?? client).post<RegisterExtensionDeviceExtensionDevicePostResponses, RegisterExtensionDeviceExtensionDevicePostErrors, ThrowOnError>({
    url: '/extension/device',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Redeem an approved pairing for a read-only token
 *
 * Redeem an approved pairing for a read-only bearer token.
 *
 * - **user_code**: the code returned when the device registered
 * - **code_verifier**: proves this is the device that started the pairing
 * - **Authentication**: none required; the verifier is the credential
 *
 * Poll this until `status` is `approved`. The token is returned exactly once and only its
 * SHA-256 is stored, so a caller that loses this response has to pair again.
 */
export const exchangeExtensionDeviceExtensionDeviceExchangePost = <ThrowOnError extends boolean = false>(options: Options<ExchangeExtensionDeviceExtensionDeviceExchangePostData, ThrowOnError>): RequestResult<ExchangeExtensionDeviceExtensionDeviceExchangePostResponses, ExchangeExtensionDeviceExtensionDeviceExchangePostErrors, ThrowOnError> => (options.client ?? client).post<ExchangeExtensionDeviceExtensionDeviceExchangePostResponses, ExchangeExtensionDeviceExtensionDeviceExchangePostErrors, ThrowOnError>({
    url: '/extension/device/exchange',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Load a pairing request for approval
 *
 * Load the facts the approval page needs before the user decides.
 *
 * - **user_code**: the code shown in the extension
 * - **Authentication**: requires authentication via access_token cookie
 *
 * Everything returned except `device_name` is vouched for by the server. `device_name` is
 * self-reported by the extension, so the page must present it as untrusted. `created_from_ip`
 * matters: a foreign address is what gives away a remote attacker who started the pairing.
 */
export const getExtensionPairingExtensionPairingUserCodeGet = <ThrowOnError extends boolean = false>(options: Options<GetExtensionPairingExtensionPairingUserCodeGetData, ThrowOnError>): RequestResult<GetExtensionPairingExtensionPairingUserCodeGetResponses, GetExtensionPairingExtensionPairingUserCodeGetErrors, ThrowOnError> => (options.client ?? client).get<GetExtensionPairingExtensionPairingUserCodeGetResponses, GetExtensionPairingExtensionPairingUserCodeGetErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/extension/pairing/{user_code}',
    ...options
});

/**
 * Approve a browser-extension pairing request
 *
 * Approve a pairing, binding it to the signed in account.
 *
 * - **user_code**: the code the user matched against their extension
 * - **Authentication**: requires authentication via access_token cookie
 *
 * No credential is issued here. The token is minted when the extension redeems the pairing,
 * so its plaintext never waits anywhere for the extension to collect it.
 */
export const approveExtensionPairingExtensionPairingUserCodeApprovePost = <ThrowOnError extends boolean = false>(options: Options<ApproveExtensionPairingExtensionPairingUserCodeApprovePostData, ThrowOnError>): RequestResult<ApproveExtensionPairingExtensionPairingUserCodeApprovePostResponses, ApproveExtensionPairingExtensionPairingUserCodeApprovePostErrors, ThrowOnError> => (options.client ?? client).post<ApproveExtensionPairingExtensionPairingUserCodeApprovePostResponses, ApproveExtensionPairingExtensionPairingUserCodeApprovePostErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/extension/pairing/{user_code}/approve',
    ...options
});

/**
 * Deny a browser-extension pairing request
 *
 * Refuse a pairing request.
 *
 * - **user_code**: the code shown in the extension
 * - **Authentication**: requires authentication via access_token cookie
 *
 * A real path rather than a timeout: someone who realises they are being phished gets a
 * deliberate way out, and the extension stops polling immediately instead of waiting for
 * the request to expire.
 */
export const denyExtensionPairingExtensionPairingUserCodeDenyPost = <ThrowOnError extends boolean = false>(options: Options<DenyExtensionPairingExtensionPairingUserCodeDenyPostData, ThrowOnError>): RequestResult<DenyExtensionPairingExtensionPairingUserCodeDenyPostResponses, DenyExtensionPairingExtensionPairingUserCodeDenyPostErrors, ThrowOnError> => (options.client ?? client).post<DenyExtensionPairingExtensionPairingUserCodeDenyPostResponses, DenyExtensionPairingExtensionPairingUserCodeDenyPostErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/extension/pairing/{user_code}/deny',
    ...options
});

/**
 * Identify the caller behind an extension token
 *
 * Return who the caller is, so the extension can label its own header.
 *
 * - **Authentication**: an extension bearer token, or an access_token cookie
 *
 * Deliberately thin. The extension is not given `/users/me`: three
 * bearer-reachable routes is a surface small enough to audit by eye, and this
 * one answers the only question the popup actually has.
 */
export const getExtensionSessionExtensionSessionGet = <ThrowOnError extends boolean = false>(options?: Options<GetExtensionSessionExtensionSessionGetData, ThrowOnError>): RequestResult<GetExtensionSessionExtensionSessionGetResponses, unknown, ThrowOnError> => (options?.client ?? client).get<GetExtensionSessionExtensionSessionGetResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }, { scheme: 'bearer', type: 'http' }],
    url: '/extension/session',
    ...options
});

/**
 * List the groups the caller belongs to
 *
 * List only the groups the caller owns or is a member of.
 *
 * - **Authentication**: an extension bearer token, or an access_token cookie
 *
 * Deliberately not `GET /groups`. That route answers with every group on the
 * instance, including other people's personal groups and each group's full
 * owner and member lists, and leaves the filtering to the browser. That is
 * fine for the web app, whose session is already as privileged as the user.
 * It is not fine for an extension token sitting in browser storage: the same
 * containment that strips the admin role from this credential would be
 * pointless if it could still map the whole organisation.
 *
 * Returns each group's id, name, whether it is personal, and whether the
 * caller owns it, which is what the popup needs to offer a group picker and
 * to decide whether to show "Add a password".
 */
export const listExtensionGroupsExtensionGroupsGet = <ThrowOnError extends boolean = false>(options?: Options<ListExtensionGroupsExtensionGroupsGetData, ThrowOnError>): RequestResult<ListExtensionGroupsExtensionGroupsGetResponses, unknown, ThrowOnError> => (options?.client ?? client).get<ListExtensionGroupsExtensionGroupsGetResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }, { scheme: 'bearer', type: 'http' }],
    url: '/extension/groups',
    ...options
});

/**
 * Disconnect every browser extension
 *
 * Disconnect every browser extension connected to the signed in account.
 *
 * - **Authentication**: requires authentication via access_token cookie
 *
 * Returns how many were still active. Already revoked and expired entries are left alone,
 * so their original timestamps survive in the audit trail.
 */
export const revokeAllExtensionTokensExtensionTokensDelete = <ThrowOnError extends boolean = false>(options?: Options<RevokeAllExtensionTokensExtensionTokensDeleteData, ThrowOnError>): RequestResult<RevokeAllExtensionTokensExtensionTokensDeleteResponses, unknown, ThrowOnError> => (options?.client ?? client).delete<RevokeAllExtensionTokensExtensionTokensDeleteResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/extension/tokens',
    ...options
});

/**
 * List the browser extensions connected to this account
 *
 * List every browser extension ever connected to the signed in account.
 *
 * - **Authentication**: requires authentication via access_token cookie
 *
 * Revoked and expired entries are included: someone checking "did I actually disconnect
 * that laptop" needs to see the answer rather than an empty list. Cookie-authenticated
 * only, so an extension token cannot enumerate the account's other devices.
 */
export const listExtensionTokensExtensionTokensGet = <ThrowOnError extends boolean = false>(options?: Options<ListExtensionTokensExtensionTokensGetData, ThrowOnError>): RequestResult<ListExtensionTokensExtensionTokensGetResponses, unknown, ThrowOnError> => (options?.client ?? client).get<ListExtensionTokensExtensionTokensGetResponses, unknown, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/extension/tokens',
    ...options
});

/**
 * Disconnect one browser extension
 *
 * Disconnect one browser extension.
 *
 * - **token_id**: the connected extension to disconnect
 * - **Authentication**: requires authentication via access_token cookie
 *
 * A token belonging to another account is reported as missing rather than forbidden, so
 * this route cannot be used to discover which token ids exist. Revoking an already
 * revoked extension succeeds without moving the original timestamp.
 */
export const revokeExtensionTokenExtensionTokensTokenIdDelete = <ThrowOnError extends boolean = false>(options: Options<RevokeExtensionTokenExtensionTokensTokenIdDeleteData, ThrowOnError>): RequestResult<RevokeExtensionTokenExtensionTokensTokenIdDeleteResponses, RevokeExtensionTokenExtensionTokensTokenIdDeleteErrors, ThrowOnError> => (options.client ?? client).delete<RevokeExtensionTokenExtensionTokensTokenIdDeleteResponses, RevokeExtensionTokenExtensionTokensTokenIdDeleteErrors, ThrowOnError>({
    security: [{
            in: 'cookie',
            name: 'access_token',
            type: 'apiKey'
        }],
    url: '/extension/tokens/{token_id}',
    ...options
});
